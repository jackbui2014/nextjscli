import React, { ReactNode } from 'react'

type Props = {
  children?: ReactNode
}

const {{name}} = ({ children? }: Props) => {
  return (
    <>
    <div>{children}</div>
    </>
  )
}

export default {{name}}

